# Salin file ini menjadi .env dan sesuaikan nilainya untuk runtime Node.js
# PIN admin digunakan oleh /api/save_manifest untuk otorisasi publikasi manifest
ADMIN_PIN=123456

# Path file manifest relatif terhadap root project (opsional)
# MANIFEST_PATH=manifest.json

# Path fallback jika manifest utama gagal ditulis (opsional)
# Default-nya akan menggunakan direktori temporary OS
# MANIFEST_FALLBACK_PATH=/tmp/pickertab-manifest.json

# Daftar host eksternal yang diizinkan untuk diproxy oleh /api/proxy
# Pisahkan dengan koma tanpa spasi berlebih
ALLOWED_HOSTS=raw.githubusercontent.com,githubusercontent.com,github.io,drive.google.com,lh3.googleusercontent.com
